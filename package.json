{
  "name": "twitdm",
  "version": "0.0.1",
  "description": "get twitter DMs so we don't have to log in to twitter.com",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "jamaicans",
  "license": "ISC",
  "dependencies": {
    "twitter-lite": "^0.14.0"
  }
}
